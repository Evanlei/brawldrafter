# Vercel Production: your Railway public URL (no trailing slash).
# Local dev: leave unset — Vite proxies /api to http://localhost:8000.
# Must include https:// (e.g. https://your-service.up.railway.app) or leave unset to use Vercel /api proxy
VITE_API_BASE=https://your-service.up.railway.app
